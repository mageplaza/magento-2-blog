<?php
/**
 * Mageplaza
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the Mageplaza.com license that is
 * available through the world-wide-web at this URL:
 * https://www.mageplaza.com/LICENSE.txt
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade this extension to newer
 * version in the future.
 *
 * @category    Mageplaza
 * @package     Mageplaza_Blog
 * @copyright   Copyright (c) Mageplaza (https://www.mageplaza.com/)
 * @license     https://www.mageplaza.com/LICENSE.txt
 */

use Hyva\Theme\ViewModel\HyvaCsp;
use Mageplaza\Blog\Block\Author\SignupForm;
use Magento\Framework\Escaper;

/** @var SignupForm $block */
/** @var Escaper $escaper */
/** @var HyvaCsp $hyvaCsp */
$author    = $block->getAuthor();
$isNew     = $author['name'] === '';
$submitTxt = $isNew ? __('Submit') : __('Save');
?>
<form enctype="multipart/form-data"
      action="<?= $escaper->escapeUrl($block->getUrl('mpblog/author/register')) ?>" method="post"
      name="mp_blog_author_form" id="blog_signup_author"
      class="blog-form"
      x-data="hyva.formValidation" @submit="onSubmit" novalidate>
    <?= /* @noEscape */ $block->getBlockHtml('formkey'); ?>

    <div class="blog-form__legend">
        <?= $escaper->escapeHtml($isNew ? __('Signup Author') : __('Author Information')) ?>
    </div>

    <div class="field field-reserved">
        <label for="name"><?= $escaper->escapeHtml(__('Display Name')) ?> *</label>
        <input type="text" name="name" id="name" value="<?= $escaper->escapeHtmlAttr($author['name']) ?>"
               title="<?= $escaper->escapeHtmlAttr(__('Display Name')) ?>"
               required data-validate='{"required": true}'/>
    </div>

    <?php if (isset($author['status'])): ?>
        <div class="field">
            <label><?= $escaper->escapeHtml(__('Status')) ?></label>
            <span class="blog-form__hint"><?= $escaper->escapeHtml($author['status']) ?></span>
        </div>
    <?php endif; ?>

    <div class="field">
        <label for="short_description"><?= $escaper->escapeHtml(__('Short Description')) ?></label>
        <textarea name="short_description" id="short_description" rows="5"><?= /* @noEscape */
            $author['short_description'] ? $block->getPageFilter($author['short_description']) : '' ?></textarea>
    </div>

    <div class="field">
        <label for="image"><?= $escaper->escapeHtml(__('Avatar')) ?></label>
        <div class="blog-form__file">
            <?php if (!empty($author['image'])): ?>
                <a class="blog-form__avatar-link" target="_blank"
                   href="<?= $escaper->escapeUrl($block->getImageUrl($author['image'], 'auth')) ?>">
                    <img class="blog-form__avatar-preview"
                         src="<?= $escaper->escapeUrl($block->getImageUrl($author['image'], 'auth')) ?>"
                         title="<?= $escaper->escapeHtmlAttr($author['image']) ?>"
                         alt="<?= $escaper->escapeHtmlAttr($author['image']) ?>"/>
                </a>
            <?php endif; ?>
            <input type="file" name="image" id="image"
                   title="<?= $escaper->escapeHtmlAttr(__('Avatar')) ?>"/>
            <?php if (!empty($author['image'])): ?>
                <label class="blog-form__file-delete" for="post_image_delete">
                    <input type="checkbox" name="image[delete]" value="1" id="post_image_delete"/>
                    <?= $escaper->escapeHtml(__('Delete Image')) ?>
                </label>
            <?php endif; ?>
        </div>
    </div>

    <div class="field">
        <label for="url_key"><?= $escaper->escapeHtml(__('URL Key')) ?></label>
        <input type="text" name="url_key" id="url_key" value="<?= $escaper->escapeHtmlAttr($author['url_key']) ?>"
               title="<?= $escaper->escapeHtmlAttr(__('URL Key')) ?>"/>
        <span class="blog-form__hint">
            <?= $escaper->escapeHtml($block->getUrl('blog/author') . $author['url_key'] . $block->getUrlSuffix()) ?>
        </span>
    </div>

    <div class="field">
        <label for="facebook_link"><?= $escaper->escapeHtml(__('Facebook Link')) ?></label>
        <input type="url" name="facebook_link" id="facebook_link"
               value="<?= $escaper->escapeHtmlAttr($author['facebook_link']) ?>"
               title="<?= $escaper->escapeHtmlAttr(__('Facebook Link')) ?>"/>
    </div>

    <div class="field">
        <label for="twitter_link"><?= $escaper->escapeHtml(__('Twitter Link')) ?></label>
        <input type="url" name="twitter_link" id="twitter_link"
               value="<?= $escaper->escapeHtmlAttr($author['twitter_link']) ?>"
               title="<?= $escaper->escapeHtmlAttr(__('Twitter Link')) ?>"/>
    </div>

    <div class="blog-form__actions">
        <button type="submit" class="blog-form__submit"
                title="<?= $escaper->escapeHtmlAttr($submitTxt) ?>">
            <?= $escaper->escapeHtml($submitTxt) ?>
        </button>
    </div>
</form>

<script>
    function initAuthorShortDescriptionEditor() {
        const shortDescriptionElement = document.getElementById('short_description');

        if (!shortDescriptionElement) {
            return;
        }

        if (!window.CKEDITOR || !window.CKEDITOR.ClassicEditor) {
            console.warn('CKEditor is not available for #short_description');
            return;
        }

        window.CKEDITOR.ClassicEditor.create(shortDescriptionElement, {
            toolbar: {
                items: [
                    'undo', 'redo', '|',
                    'heading', '|',
                    'bold', 'italic', 'strikethrough', 'underline', 'code', 'subscript', 'superscript', 'removeFormat', '|',
                    'bulletedList', 'numberedList', 'todoList', '|',
                    'alignment:left', 'alignment:right', 'alignment:center', 'alignment:justify', '|',
                    'blockQuote', '|',
                    'outdent', 'indent',
                ],
                shouldNotGroupWhenFull: true
            },
            list: {
                properties: {
                    styles: true,
                    startIndex: true,
                    reversed: true
                }
            },
            heading: {
                options: [
                    { model: 'paragraph', title: 'Paragraph', class: 'ck-heading_paragraph' },
                    { model: 'heading1', view: 'h1', title: 'Heading 1', class: 'ck-heading_heading1' },
                    { model: 'heading2', view: 'h2', title: 'Heading 2', class: 'ck-heading_heading2' },
                    { model: 'heading3', view: 'h3', title: 'Heading 3', class: 'ck-heading_heading3' },
                    { model: 'heading4', view: 'h4', title: 'Heading 4', class: 'ck-heading_heading4' },
                    { model: 'heading5', view: 'h5', title: 'Heading 5', class: 'ck-heading_heading5' },
                    { model: 'heading6', view: 'h6', title: 'Heading 6', class: 'ck-heading_heading6' }
                ]
            },
            placeholder: '',
            removePlugins: [
                'AIAssistant',
                'CKBox',
                'CKFinder',
                'EasyImage',
                'RealTimeCollaborativeComments',
                'RealTimeCollaborativeTrackChanges',
                'RealTimeCollaborativeRevisionHistory',
                'PresenceList',
                'Comments',
                'TrackChanges',
                'TrackChangesData',
                'RevisionHistory',
                'Pagination',
                'WProofreader',
                'MathType',
                'SlashCommand',
                'Template',
                'DocumentOutline',
                'FormatPainter',
                'TableOfContents',
                'PasteFromOfficeEnhanced'
            ]
        }).then(editor => {
            window.authorShortDescriptionEditor = editor;
        }).catch(err => {
            console.error(err.stack || err);
        });
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initAuthorShortDescriptionEditor, { once: true });
    } else {
        initAuthorShortDescriptionEditor();
    }
</script>
<?php $hyvaCsp->registerInlineScript() ?>
